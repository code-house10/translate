Jungle Movie Lite - Puter AI Subtitle Translation

What changed:
- Puter AI is now the main subtitle translator.
- The Translate line button and Translate all button use Puter AI first.
- MyMemory is used as fallback if Puter AI is unavailable.
- Lara is kept only as an optional backup/test setting; it is no longer the main subtitle translator.
- Puter AI still generates natural examples for Saved templates.
- Saved words, phrases, lines, templates, examples, and review progress still sync with Supabase.

Upload the full project folder to Vercel, not the HTML file only.
After deployment, use a fresh URL like ?v=puter-subtitles-v1 or clear site data to avoid old service-worker cache.
